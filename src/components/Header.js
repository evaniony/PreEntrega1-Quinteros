import NavBar from "./NavBar";

const Header = () =>{
    return(
        <header>
            <h1 className="title-nav">maceta</h1>
            <NavBar />
        </header>
    )
}

export default Header;